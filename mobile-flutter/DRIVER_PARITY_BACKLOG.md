# سجلّ فجوات تكافؤ تطبيق السائق (Flutter ↔ Expo)

> **المصدر:** تدقيق متعدّد الوكلاء (تدقيق ← تحقّق خصومي، 12 زوج شاشة) + وكيل مخصّص لطابور المال — 2026-06-30، الجلسة #16.
> التطابق على مستوى الشاشات **كامل** (12 شاشة)؛ الفجوات **داخل الشاشات**. التقرير الخام الكامل
> (الأدلّة سطر-بسطر + تبرير التحقّق الخصومي) في [`.parity/driver-audit-2026-06-30.json`](.parity/driver-audit-2026-06-30.json).
>
> **الإجمالي:** 56 فجوة واجهة/سلوك مؤكّدة (**1 عالية · 22 متوسّطة · 33 منخفضة**، 0 مرفوضة) + **5 نتائج سلامة مالية** منفصلة (قسم 0).
> **الحالة: مكتمل عملياً** (#16) — **✅ 6أ/ج/د/هـ + history‑1 + 6و + 6ب** (كل الدفعات منجزة، بما فيها idempotency الخادم).
> **المُغلَق: 55 بنداً** (MS‑1/2/3 + **52 من الـ56**). المتبقّي **3 مؤجَّلة منخفضة** (home‑4 بيانات · home‑7 إعادة هيكلة · splash-routing‑4 تصنيف إشعارات) + login‑4 غير منطبق + MS‑4/MS‑5 منخفضان.
> **`tsc` exit 0 · `flutter analyze` نظيف.** ⚠️ خطوة نشر وحيدة: **`prisma db push`** لتطبيق عمود/قيد `clientRequestId`. ثم **اختبار ميداني**.
> **القاعدة:** أغلب الفجوات قابلة للإغلاق في العميل (Flutter) دون لمس الخادم. **الاستثناء الحرج:** نتائج السلامة المالية
> MS‑1/MS‑2/MS‑4 جذرها في الخادم وتتطلّب **قراراً منك** (تعارض مع تجميد العقد — القسم 2 في `PROGRESS.md`).

الرموز: 🔴 عالية · 🟡 متوسّطة · ⚪ منخفضة · ✅ أُنجِز · 🖥️ يلمس الخادم (قرار منتج).

---

## 0) ⚠️ السلامة المالية — طابور غير المتصل (الأخطر · القسم 7)

> **✅ الحالة (بعد 6ب): الثغرة مُغلقة جذرياً.** إكمال التعبئة و«reclaim» آمنان أصلاً (حارس `updateMany` الشرطي — [`orders.service.ts`](../backend/src/orders/orders.service.ts)).
> و**walk-in / تسليم النقد** صارا idempotent عبر `clientRequestId` + قيد `@@unique([tenantId, clientRequestId])` (إضافة **متوافقة رجعياً**: Expo بلا مفتاح يبقى كما هو). MS‑3 (dedupe العميل) طبقة ثانية.
> ⚠️ **خطوة نشر مطلوبة:** تشغيل **`prisma db push`** على الخادم لتطبيق العمودين والقيدين قبل النشر (تغيير غير هدّام: عمود nullable + فهرس على صفوف كلها NULL حالياً).

- [x] 🔴🖥️ **MS‑1 — بيع walk-in صار idempotent.** ✅ #16 (الدفعة 6ب) — عمود `clientRequestId String?` + قيد `@@unique([tenantId, clientRequestId])` على `RefillOrder`؛ `createWalkinRefill` يُرجِع الصفّ القائم لنفس المفتاح (+ التقاط `P2002` للتسابق). Flutter يُولّد UUID مرّة (`newClientRequestId`) في `WalkinRefillInput`، فيُستعمل **للإرسال المباشر وللطابور معاً** → إعادة الإرسال لا تُسجّل البيع مرّتين. **متوافق رجعياً:** Expo لا يرسل المفتاح فيبقى سلوكه (Postgres يعامل NULL كمتمايز). **`tsc` exit 0 · `flutter analyze` نظيف.** ⚠️ يتطلّب `prisma db push` عند النشر.
- [x] 🔴🖥️ **MS‑2 — تسليم النقد صار idempotent.** ✅ #16 (6ب) — نفس النمط على `CashHandover` (`clientRequestId` + `@@unique` + `createForDriver` يُرجِع القائم/يلتقط P2002)؛ Flutter يُرسل المفتاح في `handoverCash`. (الحوار لا يُدرِج في الطابور اليوم؛ هذه حماية الخادم + استباق أي إدراج مستقبلي.)
- [x] 🟡 **MS‑3 — dedupe في طابور العميل (إصلاح Flutter بحت).** ✅ #16 — أُضيف عمود `dedupe_key` (هجرة v2) ومعامل `dedupeKey` في `enqueue` (يتخطّى الإدراج إن وُجد صفّ معلّق بنفس المفتاح). المفاتيح: الإكمال = `/orders/{id}/complete` (فريد للطلب)؛ البيع الفوري = `walkin:{customerId}:{amount}`. **ناقل التنفيذ لـ MS‑1 مُغلق على العميل.** [`offline_queue.dart`, `task_detail_screen.dart`, `walkin_screen.dart`].
- [ ] ⚪🖥️ **MS‑4 — تسجيل سائق مكرّر:** `registerByDriver` مجرّد `create` (يفتقد dedupe الهاتف الموجود في `submitSelfLead`). ليس مالاً (موافقة يدوية). **الحلّ:** أعِد استخدام dedupe الهاتف. [`customers.service.ts:219-248`].
- [ ] ⚪ **MS‑5 — حيوية الطابور (موروث من Expo، ليس خطأ مالياً):** `break` عند أوّل فشل شبكي مع ترتيب الأقدم-أولاً قد يوقف بقيّة الصفوف. [`offline_queue.dart:100`].

---

## 1) task-detail (7) — أُنجِز 6/7

- [x] 🟡 **task-detail-1 — توجيه reclaim.** ✅ #16 — فرع الاسترجاع (منتقي السبب + «أكّد سحب الخزان») صار يظهر من `ASSIGNED` مباشرةً (`isReclaim && (isAssigned || isEnRoute)`) بلا «ابدأ الجولة» (يقبله الخادم من ASSIGNED، كما في Expo).
- [x] 🟡 **task-detail-2 — سبب reclaim اختيار إجباري.** ✅ #16 — `_reclaimReason` صار `null` ابتدائياً (لا افتراضي)، وزرّ التأكيد مُعطَّل حتى الاختيار + نصّ «اختر سبب السحب أعلاه» + حارس داخلي في `_complete`.
- [x] 🟡 **task-detail-3 — حاجز الوصول GPS (50م) على reclaim.** ✅ #16 — أُزيل استثناء `tankReclaim`؛ فحص الـ50م يُطبَّق على الاسترجاع أيضاً عند توفّر إحداثيات الزبون (يطابق `verifyArrivalGPS` في Expo).
- [x] 🟡 **task-detail-4 — خريطة سائق حيّة + طبقة مسافة/ETA.** ✅ #16 — `_MapPreview` فوق `_TaskMap`: دبّوس السائق (أزرق) حين يتوفّر ويكون قريباً + موقع يُحدَّث كل 10ث (`_locTimer`)، وطبقة زجاجية تعرض المسافة + الوصول (تقدير ~22كم/س). الخريطة عبر `liteMode` للأداء.
- [x] ⚪ **task-detail-5 — حالة «لا GPS» للخريطة.** ✅ #16 — عند غياب إحداثيات الزبون تُعرَض حالة توضيحية («لا يوجد موقع GPS — استعمل العنوان») بدل إسقاط القسم، مع إبقاء الطبقة وزرّ الملاحة.
- [x] ⚪ **task-detail-6 — تنقّل بالعنوان النصّي.** ✅ #16 — `Launchers.navigateToAddress` (بحث خرائط Google)؛ زرّ «الملاحة» يستخدم الإحداثيات إن وُجدت، وإلا نصّ «العنوان، الحيّ، بغداد».
- [x] ⚪ **task-detail-7 — تحليلات الإتمام.** ✅ #16 — `order_completed` صار يحمل `priceIqd` + `durationSec` (من `_openedAt`)؛ وأُضيف `order_cancelled_by_driver` (orderId+reason) على مسار الإلغاء.

## 2) home (7) — أُنجِز 5/7 (+2 مؤجَّل)

- [x] 🟡 **home-1 — فرز «الأقرب أولاً» (GPS).** ✅ #16 — `nearestFirstProvider` + شريحة في ترويسة «مهام اليوم» (تُظهر «جارٍ التحديد…»)؛ عند التفعيل تُرتَّب المهام بالمسافة (`distanceMetres`)، وبلا موقع تُعيد جلب GPS وتنبّه. المهام بلا إحداثيات أخيراً (∞).
- [x] 🟡 **home-2 — شارات المسافة على البطاقات.** ✅ #16 — `driverCoordsProvider` (نبضة واحدة) + `DistanceChip` على بطاقات المتاح والمهام (تختفي بهدوء بلا GPS).
- [x] 🟡 **home-3 — بطاقة المهمّة النشطة (EN_ROUTE) المميَّزة.** ✅ #16 — `_activeTaskCard` (CTA متدرّج «قيد التنفيذ» + المسافة + «تابِع المهمة») تُفصَل أعلى القائمة.
- [x] 🟡 **home-5 — إثراء بطاقة الطلب المتاح.** ✅ #16 — صورة الحرف الأوّل + شارات (النوع · سعة الخزّان · QR) + شارة «جديد» + المسافة (من 6د). (شارة «جديد» ثابتة لا نابضة — الحركة تجميل مؤجَّل.)
- [x] ⚪ **home-6 — حوار تأكيد القبول.** ✅ #16 — بعد القبول يظهر حوار «تم قبول الطلب» بزرّ «فتح المهمة» بدل التنقّل التلقائي + snackbar (يطابق Expo).
- [ ] ⚪ **home-4 — بطاقة التقدّم اليومي (منجز/إجمالي + شريط).** ⏸️ **مؤجَّل #16:** «منجز» غير متوفّر نظيفاً في الرئيسية (`todayTasksProvider` نشطة فقط؛ العدّ المكتمل في `shiftSummaryProvider` الذي قد يُرجِع 404). يحتاج مصدر بيانات موثوقاً.
- [ ] ⚪ **home-7 — مدخل walk-in اختُزل** من بطاقة موسومة إلى أيقونة في «روابط سريعة». ⏸️ **مؤجَّل #16:** المدخل موجود ووظيفي؛ ترقيته لبطاقة بارزة إعادة هيكلة منخفضة القيمة.

## 3) shift-summary (6) — أُنجِز 6/6 ✅

- [x] 🟡 **shift-summary-1 — منع نقر مزدوج لإنهاء الوردية.** ✅ #16 — حُوِّلت الشاشة إلى `ConsumerStatefulWidget` بعلَم `_ending` (يُضبط بعد التأكيد، يعطّل الزرّ + `loading` طوال إيقاف التتبّع والتنقّل). [`shift_summary_screen.dart`].
- [x] 🟡 **shift-summary-2 — حوار التأكيد يذكّر بتسليم النقد + «غير متصل».** ✅ #16 — نصّ الحوار صار يقرّ بأن الحالة تصبح «غير متصل» ويذكّر بتسليم النقد للمعمل قبل الإنهاء.
- [x] 🟡 **shift-summary-3 — تعليق CTA المساعد.** ✅ #16 — نصّ تحت الزرّ يذكّر بإيقاف التتبّع وحالة «غير متصل» وتسليم النقد أولاً.
- [x] ⚪ **shift-summary-4 — وسم «سحب خزان».** ✅ #16 — تجاوز محلّي في `_KindRow` للاسترجاع (يطابق Expo).
- [x] ⚪ **shift-summary-5 — سحب-للتحديث.** ✅ #16 — `RefreshIndicator` حول قائمة الملخّص + `AlwaysScrollableScrollPhysics`.
- [x] ⚪ **shift-summary-6 — خصائص `shift_ended`.** ✅ #16 — تُضاف `completedOrders`/`collectedCashIqd` من `shiftSummaryProvider`.

## 4) walkin (5) — أُنجِز 5/5 ✅

- [x] 🟡 **walkin-1 — QR الخزّان معروض** في نتيجة البحث وبطاقة الزبون المختار. ✅ #16 — عبر `customer.tanks` (`Tank.qrCode`).
- [x] 🟡 **walkin-4 — لترات walk-in إلزامية بقيمة افتراضية 500.** ✅ #16 — `_litersController` يُبذَر بـ`500`، التحقّق يرفض الفراغ/≤0، وأُزيل وسم «(اختياري)» (يطابق Expo).
- [x] ⚪ **walkin-2 — صفّ «تاريخ آخر تعبئة».** ✅ #16 — `c.lastRefillAt` على بطاقة الزبون المختار.
- [x] ⚪ **walkin-3 — تذييل «نصيحة» (البحث برقم الخزّان).** ✅ #16 — صندوق نصيحة فوق حقل البحث + تحديث النصّ/الـ hint ليشملا رقم الخزّان.
- [x] ⚪ **walkin-5 — QR في عنوان نتائج البحث الفرعي.** ✅ #16 — أُضيف سطر QR الخزّان لصفّ نتيجة البحث.

## 5) profile (5) — أُنجِز 5/5 ✅

- [x] 🟡 **profile-1 — مبدّل فترة الأداء (أسبوع/شهر).** ✅ #16 — `profilePerfPeriodProvider` + `_PeriodToggle` في ترويسة بطاقة الأداء؛ العنوان والاستعلام يتبعان الفترة.
- [x] 🟡 **profile-2 — صفّ «ملخّص الوردية وإنهاؤها».** ✅ #16 — `_ActionRow` جديد → `/shift-summary` أعلى روابط الخدمة الذاتية.
- [x] ⚪ **profile-3 — رابط الشروط/الخصوصية + إفصاح الموقع.** ✅ #16 — تذييل بزرّي الشروط والخصوصية (`Launchers.openUrl`) + سطر إفصاح خصوصية الموقع.
- [x] ⚪ **profile-4 — المبلغ الكامل بدل المختصر.** ✅ #16 — `Fmt.iqd` بدل `iqdShort` في بطاقتَي الإيراد/المكافأة + `FittedBox` كي يتّسع.
- [x] ⚪ **profile-5 — شريحة الترويسة تعرض الدور «سائق معمل».** ✅ #16 — وسم ثابت بدل حالة الوردية.

## 6) earnings (6) — أُنجِز 6/6 ✅

- [x] 🟡 **earnings-5 — شرط الحالة الفارغة `total==0`.** ✅ #16 — تُحسب الإجماليات أولاً؛ عند `days.isEmpty || total==0` يُعرَض البطل (بصفر) + رسالة فارغة بدل إخفاء كل شيء (يطابق Expo).
- [x] ⚪ **earnings-1 — قيمة فوق كل عمود (الأسبوع).** ✅ #16 — `${(totalIqd/1000).round()}ك` فوق العمود (مع تقليص `maxBarHeight` لاستيعابها).
- [x] ⚪ **earnings-2 — نموّ الأعمدة من الصفر.** ✅ #16 — `TweenAnimationBuilder` بتأخير متدرّج بسيط حسب الفهرس.
- [x] ⚪ **earnings-3 — دخول البطاقات (FadeInDown).** ✅ #16 — `_FadeSlideIn` (تلاشٍ + انزلاق) حول البطل والمخطّط.
- [x] ⚪ **earnings-4 — اهتزاز عند تبديل الفترة.** ✅ #16 — `Hap.tap()` في `onSelectionChanged`.
- [x] ⚪ **earnings-6 — العنوان الفرعي.** ✅ #16 — «عمولاتك وبونصك يوماً بيوم» تحت العنوان في الـ AppBar.

## 7) login (4) — أُنجِز 3/4 (+1 غير منطبق)

- [x] 🟡 **login-1 — رسالة خطأ 401 خاصّة بالدخول.** ✅ #16 — مساعِد `_loginError` (نمط الزبون): 401 → «بيانات الدخول غير صحيحة. تواصل مع المعمل لإعادة تعيين كلمة المرور».
- [x] ⚪ **login-2 — رسالة 429 بـ«١٥ دقيقة».** ✅ #16 — ضمن `_loginError` (429 → «محاولات كثيرة. حاول بعد ١٥ دقيقة»).
- [x] ⚪ **login-3 — الزرّ يعكس الصحّة الحيّة.** ✅ #16 — `_canSubmit` + مستمعا الحقلين؛ زرّ الدخول مُعطَّل حتى صحّة الهاتف وكلمة المرور.
- [ ] ⚪ **login-4 — زرّ دخول سريع تطويري** (`__DEV__`، كريم). ❎ **غير منطبق #16:** مكافئه في Flutter هو **وضع العرض** (`Env.demoMode` + `DemoInterceptor`)؛ أداة مطوّر لا تُشحَن للإنتاج — لا حاجة لتكرار نسخة Expo.

## 8) van-inventory (5) — أُنجِز 5/5 ✅

- [x] 🟡 **van-inventory-1 — حارس حالة «متّسخة».** ✅ #16 — حُفِظت قيم الخادم (`_serverFull/_serverEmpty`) و`_dirty` يقارنها بالحالية؛ زرّ الحفظ مُعطَّل وتُمنع POST اللا-عمل حتى يتغيّر شيء (يُحدَّث المرجع بعد الحفظ). [`van_inventory_screen.dart`].
- [x] 🟡 **van-inventory-3 — تنبيه نجاح + رجوع تلقائي.** ✅ #16 — حوار «تم الحفظ» ثم `context.pop()` للرجوع للشاشة السابقة (يطابق Expo).
- [x] ⚪ **van-inventory-2 — نصّ «لا توجد تغييرات لحفظها».** ✅ #16 — يظهر تحت الزرّ عند `!_dirty` (مكافأة مع van-inventory-1).
- [x] ⚪ **van-inventory-4 — اهتزاز.** ✅ #16 — `Hap.tap()` على خطوات العدّادَين + `Hap.success()/error()` حول الحفظ.
- [x] ⚪ **van-inventory-5 — وسم «جارٍ الحفظ…».** ✅ #16 — أُضيف `loadingLabel` لـ `LoadingButton` (مغزل + نصّ) ويُمرَّر هنا.

## 9) splash-routing (4) — أُنجِز 3/4 (+1 مؤجَّل)

- [x] 🟡 **splash-routing-1 — شريط «غير متصل» العام.** ✅ #16 — أُضيف `_AppChrome` في `main.dart` (فوق الـ router) يعرض الشريط الأحمر على كل الشاشات (نمط `_OfflineWrapper` للزبون). *(مكافأة مع splash-routing-3.)*
- [x] 🟡 **splash-routing-3 — تفريغ الطابور على مستوى التطبيق.** ✅ #16 — نُقِل المؤقّت (60ث) + مستمع عودة الاتصال من `home_screen` إلى `_AppChrome`، فيُصرَّف الطابور حتى على شاشة المهمة/النقد/البيع. [`main.dart`, `home_screen.dart`].
- [x] ⚪ **splash-routing-2 — حدث `login_success`.** ✅ #16 — `Analytics.capture('login_success', {role:'driver'})` بعد نجاح الدخول.
- [ ] ⚪ **splash-routing-4 — نقر الإشعار يوجّه على أي `orderId`.** ⏸️ **مؤجَّل #16:** يحتاج تأكيد قيمة `type` التي يرسلها الخادم للإشعار «طلب جديد» مقابل غيرها؛ الحارس بقيمة خاطئة يكسر فتح كل الإشعارات (أسوأ من السلوك الحالي المتساهل). يُغلَق بعد تثبيت تصنيف أنواع الإشعارات.

## 10) history (2) — أُنجِز 2/2 ✅

- [x] 🟡 **history-1 — اسم الزبون كسطر رئيسي.** ✅ #16 — أُضيف `RefillOrder.customerName` (من `customer.fullName`؛ **تحقّق خصومي:** `listMyHistory` يضمّ `customer: true` و`Customer.fullName` حقل scalar — لا تغيير على الخادم). بطاقة السجلّ تعرض الاسم (بديل «— مجهول —») والنوع+التاريخ ثانوياً.
- [x] ⚪ **history-2 — التاريخ فقط بدل التاريخ+الوقت.** ✅ #16 — `Fmt.arabicDate` بدل `Fmt.arabicDateTime` في بطاقة السجلّ.

## 11) cash (3) — أُنجِز 3/3 ✅

- [x] ⚪ **cash-1 — تاريخ فقط.** ✅ #16 — `Fmt.arabicDate` في صفّ التسليم.
- [x] ⚪ **cash-2 — اهتزاز نجاح/خطأ.** ✅ #16 — `Hap.success()/error()` في `_submit`.
- [x] ⚪ **cash-3 — هالة نابضة للنقد المعلَّق.** ✅ #16 — `_PulseGlow` حول بطاقة «المتبقّي» عند `pending>0`.

## 12) forgot (2) — أُنجِز 2/2 ✅

- [x] 🔴 **forgot-1 — دخول تلقائي بعد إعادة التعيين.** ✅ #16 — صار يستدعي `authControllerProvider.notifier.resetPasswordAndLogin()` (يوثّق حالة المصادقة في الذاكرة) ويوجّه إلى `/home` بدل `/login` (نفس إصلاح الزبون #10).
- [x] ⚪ **forgot-2 — «إعادة الإرسال» يرجع للخطوة 1 ويمسح.** ✅ #16 — زرّ «لم يصلك الرمز؟ أعِد المحاولة» يرجع لـ`_step=1` ويمسح الرمز/كلمة السر (يطابق الزبون).

---

## خطّة الدفعات المقترحة (الأخطر أولاً)

| الدفعة | المحتوى | يلمس الخادم؟ |
|---|---|---|
| **6أ — السلامة المالية (العميل) ✅ #16** | MS‑3 (dedupe الطابور) + splash-routing‑1/3 (شريط + تفريغ عام) + shift-summary-1 (منع نقر مزدوج) + van-inventory‑1/2 (حارس متّسخ + نصّ) — `flutter analyze` نظيف | لا |
| **6ب — idempotency الخادم ✅ #16** | MS‑1 + MS‑2: عمود `clientRequestId` + `@@unique([tenantId, clientRequestId])` على `RefillOrder`/`CashHandover` + منطق إرجاع-القائم/التقاط P2002 + Flutter يُرسل المفتاح. `tsc` exit 0 · `flutter analyze` نظيف. **⚠️ يحتاج `prisma db push` عند النشر.** (MS‑4 منخفض، مؤجَّل) | نعم 🖥️ (إضافي/متوافق رجعياً) |
| **6ج — تدفّقات حرجة ✅ #16** | forgot-1 (دخول تلقائي) + task-detail‑1/2/3 (توجيه reclaim + إجبار السبب + حارس 50م) — `flutter analyze` نظيف | لا |
| **6د — ميزات GPS/الخريطة ✅ #16** | home‑1/2/3 (فرز قريب + مسافة + بطاقة نشطة) + task-detail‑4/5/6 (خريطة حيّة + ETA + بدائل لا‑GPS) — `flutter analyze` نظيف | لا |
| **6هـ — لمسات الشاشات ✅ #16** | 10 متوسّطة (home‑5 · profile‑1/2 · earnings‑5 · walkin‑1/4 · login‑1 · van-inventory‑3 · shift-summary‑2/3) + 4 منخفضة مكافأة (login‑2 · walkin‑2/5 · history‑2) — `flutter analyze` نظيف | لا |
| **history‑1 (موديل) ✅ #16** | `RefillOrder.customerName` (من `customer.fullName`، تحقّق خصومي بالعقد) + عرضه في بطاقة السجلّ — `flutter analyze` نظيف | لا |
| **6و — تجميل ✅ #16** | 22 بنداً: cash‑1/2/3 · earnings‑1/2/3/4/6 · van-inventory‑4/5 · shift-summary‑4/5/6 · profile‑3/4/5 · walkin‑3 · login‑3 · splash-routing‑2 · home‑6 · task-detail‑7 · forgot‑2 — `flutter analyze` نظيف. **مؤجَّل:** home‑4/7 · splash-routing‑4 · login‑4 (غير منطبق) | لا |

> بعد كل دفعة: `flutter analyze` نظيف + تحديث هذا السجلّ + سطر في `PROGRESS.md`.
