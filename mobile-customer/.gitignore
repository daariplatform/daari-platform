node_modules/
.expo/
dist/
web-build/
# android/ and ios/ are committed (bare workflow with prebuild output
# checked in). This guarantees EAS Build uses exactly the same Gradle
# settings we tested locally, instead of re-running prebuild on the
# cloud where the template can drift between releases.
android/build/
android/app/build/
android/.gradle/
android/local.properties
ios/build/
ios/Pods/
.env
.env.local
*.log
.DS_Store
google-service-account.json
GoogleService-Info.plist

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli