# ✅ قائمة مهام التكافؤ الكامل: Flutter ⟵ Expo — **v4 (إعادة تدقيق عميقة)**

> الهدف: رفع نسخة Flutter (`mobile-flutter/`) إلى **طبق الأصل** من نسختَي Expo
> (`mobile-customer/` + `mobile-worker/`).
>
> **v4:** إعادة تدقيق عميقة بـ ٦ وكلاء خصاميين بعد آخر التحديثات (شملت commits
> صاحب المشروع `d151ac8` / `8dbb749` / `d0a6800`). كُشِفت **~٣٠ فجوة على مستوى
> الشاشات** لم تُرصد سابقاً (٣ منها عالية). آخر تحديث: 2026-06-03.

## 🎯 حالة التنفيذ

| الطبقة | المحتوى | الحالة |
|---|---|---|
| المستويات ١–٣ (جلستي) | الخريطة الحيّة · الطابور · «انتقلت» · حاجز ٥٠م · Sentry · PostHog · haptics · المكوّنات المتحركة · demo · history sections · روابط الإعدادات · onboarding | ✅ |
| `d151ac8` (صاحب المشروع) | كاش بارد · عدّادات لكل شاشات السائق · skeleton/shimmer · إعدادات الإشعارات | ✅ |
| **v4: فروقات جديدة مكتشفة** | ٣ عالية · ~١١ متوسطة · ~١٤ منخفضة | ⏳ مفتوحة |

> رمز: 🔴 عالية · 🟠 متوسطة · 🟡 منخفضة · ✅ منجز · 🔵 ليست فجوة (تحقّق)

---

## 🔴 المستوى ١ — فجوات عالية (مُتحقَّقة)

- [ ] 🔴 **`<queries>` ناقص في AndroidManifest (كلا التطبيقين)** — *الأخطر*. الـ manifestان يعلنان `PROCESS_TEXT` فقط (`apps/customer/.../AndroidManifest.xml:52-57` + driver نفسه). على Android 11+ يُرجِع `canLaunchUrl` **false** لـ `tel:`/`https`(wa.me, maps)/`mailto`، فكل `Launchers.*` في `launchers.dart:26-30` **تفشل بصمت**. يكسر: اتصال السائق بالزبون + واتساب + «ابدأ الملاحة» (تدفّق ميداني أساسي) واتصال الزبون + روابط الدعم والقانونية. **الحل:** أضِف `<queries>` لـ tel/https(+BROWSABLE)/geo/mailto، أو أزِل بوابة `canLaunchUrl`.
- [ ] 🔴 **حذف الحساب غائب في بروفايل السائق** (`DELETE /auth/me`) — شرط متجر Apple/Google. موجود في الزبون (`settings_screen.dart:200`) والدالة جاهزة (`auth_repository.deleteAccount()`)، لكن `apps/driver/lib/screens/profile_screen.dart` لا يعرضه.
- [ ] 🔴 **عدم استئناف تتبّع GPS للسائق عند إعادة فتح التطبيق** — Expo يبدأ التتبّع تلقائياً لكل سائق مسجَّل عند كل إقلاع (`mobile-worker/app/_layout.tsx:71-75`). في Flutter: `splash_screen.dart` يكتفي بالاستعادة، والتتبّع يبدأ فقط عند الدخول الصريح أو يدوياً من مفتاح الوردية. سائق يعيد فتح التطبيق يبقى **OFFLINE وغير متتبَّع** (اللوحة لا ترى موقعه) حتى يقلب المفتاح يدوياً.

---

## 🟠 المستوى ٢ — فجوات متوسطة

- [ ] 🟠 **منتقي العنوان المحفوظ على الرئيسية + تمرير `addressId`** — Expo يعرض شريط عناوين ويمرّر العنوان المختار للطلب. Flutter `home_screen.dart` يستدعي `createRefill(customerId)` بلا `addressId` رغم دعم الـ repo له → الزبون متعدّد العناوين لا يختار وجهة التوصيل.
- [ ] 🟠 **حارس الطلب النشط (منع التكرار) على الرئيسية** — Flutter يعرض `_orderCard` بزرّ «اطلب الآن» مفعّلاً حتى مع وجود طلب جارٍ؛ المنع من الخادم فقط. Expo يحوّل الزرّ إلى «تتبّع طلبك الحالي».
- [ ] 🟠 **بوّابة `customerConfirmedAt` في تفاصيل الطلب** — Flutter يعرض «أكّد الاستلام»/«أبلغ» دائماً لأي طلب مكتمل (النموذج `RefillOrder` يفتقر للحقل أصلاً) → إمكان تأكيد مكرّر ولا حالة «تم التأكيد، شكراً».
- [ ] 🟠 **زرّ «افتح في خرائط Google» تحت خريطة طلب الزبون** — الخريطة المضمّنة موجودة، لكن لا زرّ ملاحة خارجية للوجهة (`Launchers.navigate` موجود ومستخدم لدى السائق فقط). *(خاضع أيضاً لباگ `<queries>` أعلاه.)*
- [ ] 🟠 **منتقي عنوان لكل جدولة** — Expo يربط الجدولة بعنوان محفوظ ويعرضه؛ Flutter `schedules_screen.dart` يجمع التواتر فقط رغم دعم `ScheduleInput.addressId`.
- [ ] 🟠 **تثبيت GPS لعنوان محفوظ (map-picker)** — Expo يلتقط lat/lng للعنوان؛ Flutter `addresses_screen.dart` يجمع نصّاً فقط رغم دعم `AddressInput` للإحداثيات → دقّة توصيل أضعف للعناوين غير الافتراضية.
- [ ] 🟠 **رئيسية السائق: فرز «الأقرب أولاً» + رقاقات المسافة** — Expo يفرز المهام بالأقرب (haversine من GPS) ويعرض مسافة كل بطاقة؛ Flutter يعرض ترتيب الخادم الخام بلا مسافات (الإحداثيات متوفّرة في `DriverTask`).
- [ ] 🟠 **خريطة مهمة السائق: علامة السائق الحيّة + مسافة/ETA** — `_CustomerMap` (liteMode) يعرض علامة الزبون فقط؛ Expo يضيف علامة السائق الحيّة وتراكب مسافة+ETA. *(الملاحة وحاجز ٥٠م موجودان.)*
- [ ] 🟠 **شريط عدم الاتصال عام (كلا التطبيقين)** — Expo يركّب `OfflineBanner` فوق كل الشاشات (`_layout.tsx`). Flutter: `_syncBanner` داخل رئيسية السائق فقط، **والزبون بلا شريط إطلاقاً** (رغم وجود `isOnlineProvider`). الشاشات التي تُجري كتابات أوفلاين (walkin/task/cash) بلا مؤشّر.
- [ ] 🟠 **PostHog: أحداث ناقصة** — Flutter يلتقط ٤ فقط (`order_placed`/`order_claimed`/`order_completed`/`shift_ended`). مفقودة: `login_success`/`login_failed`/`signup_started`/`signup_completed`/`order_cancelled_by_driver`.
- [ ] 🟠 **demo mode للسائق ناقص** — `DemoInterceptor` بلا فرعَي `GET /customers` (بحث الزبائن → نتائج فارغة في walk-in) و`GET /drivers/me/perf` (شاشة الأداء تعرض أصفاراً بدل بيانات تجريبية).
- [ ] 🟠 **Haptics: تغطية الزبون ناقصة** — `Hap` موصول في رئيسية الزبون فقط (place-order). مفقود في: login/orders/تقييم/addresses/schedules/support/intro. *(ادّعاء v-سابق «claim موصول للزبون» غير دقيق — لا claim في تطبيق الزبون.)*

---

## 🟡 المستوى ٣ — فجوات منخفضة (تلميع/راحة)

- [ ] 🟡 **عدّاد تنازلي للعرض** على الرئيسية (Expo: «ينتهي خلال X ساعة») — Flutter يعرض نسبة الخصم الثابتة فقط.
- [ ] 🟡 **معاينة الإيصال السريعة** للطلبات المكتملة (Expo: زرّ «الإيصال» يوسّع تفاصيل) — Flutter «إعادة الطلب» فقط.
- [ ] 🟡 **تقسيم الطلبات بالشهر** (Expo: رؤوس «مايو ٢٠٢٦») — Flutter قائمة مسطّحة.
- [ ] 🟡 **دخول تلقائي بعد إعادة تعيين كلمة السر** — Expo يسجّل الدخول مباشرة؛ Flutter يعيد لشاشة الدخول.
- [ ] 🟡 **reverse-geocode في map-picker** لتعبئة نصّ العنوان تلقائياً — Flutter يرجع `LatLng` خاماً فقط.
- [ ] 🟡 **تعديل تاريخ أول تشغيل للجدولة ±يوم** — Flutter للقراءة فقط.
- [ ] 🟡 **مبدّل أسبوع/شهر للأداء** في بروفايل السائق — Flutter يثبّت `'month'`.
- [ ] 🟡 **رابط الخصوصية/الشروط في بروفايل السائق** — موجود في الزبون فقط.
- [ ] 🟡 **بطاقة تقدّم اليوم + إبراز المهمة النشطة** على رئيسية السائق — Flutter بطاقات متماثلة بلا ملخّص.
- [ ] 🟡 **رقاقات سعة الخزان/QR على بطاقة الطلب المتاح** (السائق) — Flutter يسقطها رغم توفّرها في `TaskTank`.
- [ ] 🟡 **متتبّع خطوات أفقي** في مهمة السائق (قبول→طريق→وصل→تعبئة→تأكيد) — Flutter شارة حالة واحدة.
- [ ] 🟡 **حارس «لا تغييرات لحفظها»** في جرد الفان — Flutter يُمكّن الحفظ دائماً.
- [ ] 🟡 **Haptics للسائق** في cash/earnings/van/walk-in — مفقودة.
- [ ] 🟡 **Sentry: `beforeBreadcrumb` (تجريد التوكنات) + `release`** غير منقولَين (تعليق `crash_reporting.dart` يدّعي المطابقة خطأً). الأثر صغير لعدم ربط Sentry-Dio.
- [ ] 🟡 **`isOnline`: حضور واجهة مقابل وصول فعلي للإنترنت** — Flutter يعدّ أي واجهة «متصل»؛ Expo يفحص `isInternetReachable` (WiFi ميت = أوفلاين).
- [ ] 🟡 **pull-to-refresh في ملخّص وردية السائق** — مفقود (موجود في بقية الشاشات).
- [ ] 🟡 **بديل البريد (mailto) في شاشة الدعم** — Flutter واتساب فقط.

---

## ✅ أُنجِز (للمرجع)

كل بنود المستويات ١–٣ من جلستي + commit `d151ac8` لصاحب المشروع:
الخريطة الحيّة · الطابور الأوفلاين + التصريف عند العودة · «انتقلت» · حاجز ٥٠م ·
Sentry · PostHog (٤ أحداث) · المكوّنات المتحركة (Rain/RefillStrip/RecentActivity) ·
demo mode · تقسيم سجلّ السائق · روابط الإعدادات القانونية · onboarding ·
**skeleton/shimmer (✅ d151ac8)** · **AnimatedCounter لكل شاشات السائق (✅ d151ac8)** ·
**تبديل إذن الإشعارات (✅ d151ac8)** · **كاش بارد جزئي (🟡 d151ac8 — `response_cache`+`cache_interceptor`، شبكة-أولاً مع رجوع للكاش؛ ليس رسماً فورياً مثل `persist.ts`)**.

## 🔵 ليست فجوات (تحقّق)

- **i18n / RTL** — Flutter يطابق عبر `Directionality` + locale `ar`.
- **PartnerAds · WaterDropHero · TankStatusCard · Icon3D** — كود ميت في Expo (غير مستخدم).
- **StarRating · EmptyState** — موجودان في Flutter.
- **`/customers/me/onboard` · `/uploads/proof`** — الأولى غير موجودة بالباك إند (404 في Expo أيضاً)؛ الثانية كود ميت في Expo (أُزيل إثبات الصورة).

---

## خلاصة v4

سطح الـ API والبنية والمكوّنات الكبرى **مكتملة**. المتبقّي **فروقات على مستوى الشاشات** — أهمّها ٣ عالية يجب حسمها قبل الإطلاق:
1. **`<queries>` في الـ manifest** (يكسر الاتصال/الواتساب/الملاحة بصمت على Android حديث).
2. **حذف حساب السائق** (شرط متجر).
3. **استئناف تتبّع GPS عند إعادة فتح تطبيق السائق**.
